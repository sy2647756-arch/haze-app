# Mood Quiz — v1 设计决策记录

> 本文件是《功能设计文档_完整版.docx》的**修订与收敛记录**，产生于开发前的一轮设计拷问。
> 原始设计文档保留不动；本文件记录「相对原文档改了什么、v1 到底做什么」，作为写代码的直接依据。
> 凡本文件与原文档冲突处，**以本文件为准**。
>
> 生成日期：2026-07-18

---

## 0. 全局定位（推翻/收敛原文档的隐含假设）

| 项 | 决定 | 说明 |
|---|---|---|
| 有效文档 | 只看《完整版》 | 《功能1_分享答题双人报告.docx》与完整版 MD5 完全相同，是重复文件，忽略 |
| 目标市场 | **海外** | 英文 UI 保留；地图/支付/大模型全部换海外方案（见 §1） |
| 引流方式 | 短视频（TikTok/Reels）把陌生人带到站内 | Co-op 链接是 **A→B 私密定向分享**，报告是给当事人看的，**不是**对外传播图；Figma 横向对比表保留 |
| 开发顺序 | 按原文档附录 | 账户 → 日记 → Report → Quiz Solo → Co-op → Healing → My/订阅 |
| **v1 范围** | **账户 + 日记 + Report** | Quiz / Co-op / Healing / My·订阅 全部推迟 |

---

## 1. 技术栈修正（因「海外」定位产生）

| 原文档 | 改为 | 理由 |
|---|---|---|
| 高德 Maps API | **Google Maps / Places** | 海外定位 |
| 微信 / 支付宝支付 | **Stripe** | RevenueCat 仅当有原生 App 时才加，纯 Web 用不上 |
| 通义千问 / DeepSeek | **Claude 或 GPT** | 英文语境；海外可用性/延迟更优 |
| 保留不变 | Flutter Web · Supabase · Vercel · go_router · 英文 UI | — |

> 注：v1 不涉及支付和大模型（那些在 Healing / 订阅模块）。地图仅日记地点用到。

---

## 2. 账户模块（v1）

**核心决定：延迟注册 —— 落地即用，攒下数据后再绑账户。彻底取消注册墙。**

| 项 | 决定 |
|---|---|
| 落地 | 前端静默 `signInAnonymously()`，建真实 `auth.users` 行；**零字段、零墙**，直接写日记 |
| 数据存储 | 匿名 `user_id` 是真的 → RLS、外键、云端存储**全部照常**，日记存云端不是本地 |
| 绑定方式 | **仅 Google 一键**（`linkIdentity()` 挂到同一 `user_id`，历史日记无缝保留，无数据迁移） |
| 绑定时机 | 软提示：① 写满第 3 篇日记 ② 进入 Report 页；硬绑定：将来 Co-op / 订阅 |
| 昵称 `nickname` | 移到**绑定那一步**才问；落地不问（首页用默认称呼或不称呼） |
| 头像 `avatar_url` | 可跳过（「稍后再说」） |
| `gender` / `relationship_stage` / `partner_name` | **列保留**（将来用），**v1 不采集**。`partner_name` 待 Co-op 上线时问 |
| 跨设备 | 绑定前换设备/清缓存 = 看不到旧数据，**接受**，靠绑定提示兜底，v1 不额外缓解 |

> ⚠️ 匿名 session 凭证在本设备 localStorage，清缓存/换设备即永久失联 → 绑定提示是**必须**的，不是可选。

---

## 3. 日记模块（v1 核心）

| 项 | 决定 | 相对原文档 |
|---|---|---|
| 粒度 | **一天一篇**，`UNIQUE(user_id, date)` | 原文档 Figma 示例有一天两篇，属笔误，改掉 |
| 补写 | **最近 7 天滚动窗口**可补，超出不可补 | 原文档未定义补写 |
| 补写标记 | 补写的行 `is_backfilled = true` | 新增字段（现在免费，将来回填贵） |
| 情绪输入 | **只选天气**；`mood_score` 由天气**直接映射**得出 | 原文档的独立滑条取消 |
| `mood` 字段 | **删除** | 无输入口、无展示位、无消费方，与 `mood_score` 语义重叠 |
| `Day N` | **取消**，日记头只显示日期 | 原文档 Figma 的「Day 4」去掉 |
| 草稿 | 本地存（localStorage），`key = draft_{user_id}_{date}`，一天一份互不覆盖；提交成功后删；超 7 天窗口自动清 | 原文档未提 |
| 首页 | 显示今天那唯一一篇；没有则「今天还没写」 | 原文档「若有则显示」的单条假设恢复有效 |

### 3.1 `weather` 枚举（闭合，7 个，存英文 key）

> 存 **key**（不存 emoji / 中文）；emoji 与文案前端映射。天气**必填**。
> score 映射范围 **1–7**（每个天气一档，一一对应；折线图在 7 个固定高度上跳）。**已确认定稿。**

| # | weather (key) | emoji | score | 效价 |
|---|---|---|---|---|
| 1 | `stormy` | ⛈️ | 1 | 崩溃 / 愤怒 |
| 2 | `rainy` | 🌧️ | 2 | 低落 / 难过 |
| 3 | `haze` | 🌫️ | 3 | 迷茫（中性偏下） |
| 4 | `cloudy` | ☁️ | 4 | 平淡 |
| 5 | `snowy` | ❄️ | 5 | 孤寂但平静 |
| 6 | `breezy` | 🍃 | 6 | 轻松 |
| 7 | `sunny` | ☀️ | 7 | 愉悦 |

---

## 4. 地点模块（v1，属日记）

| 项 | 决定 | 相对原文档 |
|---|---|---|
| 选点方式 | **搜索 + 附近列表**（Google Places Autocomplete + Nearby） | **「地图点选」砍掉**（原难度最高、无独占价值） |
| 地图区域 | 纯展示（看自己在哪），不承担取点 | — |
| 存储字段 | `location_name` + `latitude` + `longitude`（POI 自带坐标，顺手存） | 坐标保留（原本一度考虑删，因选了「保留地点」而留下） |
| API 安全 | Google Places JS key 必须配 **HTTP referrer 白名单 + 配额上限** | 原文档缺此防线，补上（JS key 藏不住，只能限域名+限量） |
| 附近列表 | 需**去重**（同名同址折叠），标 `≤100m` 距离 | 原文档未提（Places 真实返回大量重复条目） |
| 底图 | Google Maps，确认英国等地区渲染英文 | Figma 里的中文标签是截图底图残留 |

---

## 5. 媒体模块（v1，属日记）

| 项 | 决定 |
|---|---|
| 存储 | Supabase Storage **public bucket**，`diaries.image_urls` 只存 URL 数组 |
| 删除 | 删/改日记时**服务端显式删除** Storage 文件；编辑时 diff 新旧数组删掉被移除的 |
| 顺序 | **先删文件、成功后再删数据库行**（反过来会导致文件 URL 永久失联） |
| ⚠️ 隐私债 | public bucket 漏删的文件凭原 URL 全网可访问、不过期 → 属 GDPR 删除权敞口。v1 用户量小可接受，**上量前必须迁 private bucket + 签名 URL** |

---

## 6. Report 模块（v1）

**四宫格 → 方案「丙」：日历 + 折线图 + 成就（仅日记类），认知偏差块 v1 不上。**

| 块 | v1 状态 | 说明 |
|---|---|---|
| 日历 | ✅ | `table_calendar` **两周视图**，`⌄` 展开整月；格子 = `weather` emoji |
| 折线图 | ✅ | `mood_score`，`fl_chart` |
| 成就 Achievements | ✅ 仅日记类 | v1 数据源只有日记行为（首篇、连续 3/7/30 天、累计 N 篇、首次补写、集齐 N 种天气……）；streak **认补写**（窗口仅 7 天，注水有限） |
| 认知偏差 Cognitive Bias Insights | ❌ v1 不渲染 | 数据源 `quiz_results` 由 Quiz 模块写，v1 无写入方 → 整块拿掉，等 Quiz 落地补 |

### 6.1 日历四态

| 日子 | 显示 | 可点 |
|---|---|---|
| 有日记 | `weather` emoji | ✅ → Detail |
| 今天、未写 | `+`（实心高亮） | ✅ → 写日记 |
| 过去 7 天内、未写 | `+`（灰色） | ✅ → 补写 |
| 超 7 天 / 未来 | 虚线空心圆 / 淡日期 | ❌ |

### 6.2 折线图画法（关键，易踩坑）

- X 轴 = **真实日期时间戳**（`fl_chart` 的 x 用日期，**绝不能用数组下标**——用下标会把时间轴压平，谎报趋势）。
- 相邻点间隔 **≤ 3 天**：连**实线**。
- 相邻点间隔 **> 3 天**：画**虚线**（诚实表达「这段没数据、趋势是猜的」）。
- 设计验收用「一周写 3 天」的稀疏数据看，别拿满格数据验收。

---

## 7. 相对原文档的「删除 / 推迟」总清单

**永久删除（无消费方）：**
- `diaries.mood` 字段
- 「地图点选」交互
- （账户）注册墙 / 强制填资料页

**v1 推迟（列/功能保留，以后做）：**
- Quiz Solo / Co-op / Healing / My·订阅 全模块
- Report 的认知偏差块
- `profiles` 的 `gender` / `relationship_stage` / `partner_name` 采集
- private bucket 迁移（上量前）

---

## 8. 待最终确认项（TODO，未在拷问中拍死）

- [x] ~~`weather` 7 个枚举、`snowy` 为第 7 个、score 1–7 映射、效价排序~~ —— **已确认定稿**（见 §3.1）
- [ ] 折线图时间范围是否需要周期切换（周/月/全部）——未拷问
- [ ] 成就徽章的具体清单与解锁阈值——未拷问
- [ ] Report「导出/分享」（原功能五最后一条）是否进 v1——未拷问
- [ ] Co-op 的 `/together/{id}` OG 预览方案（Edge Function 注入 per-session meta）——非 v1，但记为已知技术债

---

## 9. `diaries` 表结构（v1 定稿）

```
diaries
  id            uuid    pk
  user_id       uuid    -> auth.users, RLS 只读写自己
  date          date    UNIQUE(user_id, date)
  weather       text    枚举 key: stormy/rainy/gloomy/hazy/breezy/sunny/bright
  mood_score    int     1-7, 由 weather 映射（派生但落库，方便查询/画图）
  content       text
  image_urls    jsonb   URL 数组，指向 public bucket
  location_name text    nullable
  latitude      float8  nullable
  longitude     float8  nullable
  is_backfilled bool    default false
  created_at    timestamptz
  -- 已删除：mood
```

RLS：`user_id = auth.uid()` 全表行级。
