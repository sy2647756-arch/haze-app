# Mood Quiz — 开发交接文档

> 用途：在**新对话**里继续开发时，把本文件（尤其是最后「§9 继续开发」那段）贴给助手，即可无缝接手。
> 最后更新：2026-07-22

---

## 1. 一句话现状

Flutter Web 情绪/关系 App，已上线 GitHub Pages。**v1 四个 Tab（Weather mood / Healing / Report / My）全部完成**，Healing 的两个子功能（2-min Meditation、12h Calm Drawer）已做。数据本地存储（`shared_preferences`），**尚未接 Supabase**。

- **线上地址**：https://usercjr.github.io/mood_quiz/
- **仓库**：https://github.com/usercjr/mood_quiz （公开，`main` 分支）
- **本地路径**：Flutter 工程在 `D:\projectd\mood_quiz\mood_quiz\`（注意是**双层** mood_quiz）

---

## 2. 环境 & 运行

- Flutter 3.38 / Dart 3.12，Windows，PowerShell。
- **本地跑（Web，务必带 `--no-web-resources-cdn`，否则某些网络下 CanvasKit 从 gstatic CDN 拉取会白屏）**：

```bash
cd D:\projectd\mood_quiz\mood_quiz
flutter run -d web-server --web-port 8977 --web-hostname 127.0.0.1 --no-web-resources-cdn
```

打开 `http://127.0.0.1:8977`。改代码后需**重启**该命令（资源变更热重载不生效）。

- 静态检查 & 测试：`flutter analyze`（须 0 问题）、`flutter test`（当前 **27 通过**）。
- **推送即自动部署**：`.github/workflows/deploy.yml` 在 push 到 main 时用 GitHub Actions 构建并发布到 `gh-pages`。已配好 `permissions: contents:write` 和 `--no-web-resources-cdn`。

---

## 3. 关键约束（务必遵守）

1. **统一手机画框**：`main.dart` 的 `_PhoneFrame` 把所有页面约束为 **393×852** 居中等比缩放。**新页面直接按 393×852 设计坐标布局，不要自己再做缩放**。
2. **像素还原 Figma**：所有 UI 严格按 Figma 设计还原（坐标/颜色/字号）。
   - **当前设计文件**：`APP_Haze`，fileKey = `ldx4kyok408yp6dMgDBy0F`（含 Quiz / Onboarding / Co-op 邀请 / 订阅 / 首页精修等，以此为准）。
   - 旧文件 `0s0WuXr07bACqY0iLpji8q` 是最初的设计（日记/Healing/Report/My 等已实现页面来自它），仅作历史参考。
3. **Web 滚动坑**：Flutter Web 默认鼠标拖不动滚动组件（PageView / ListWheelScrollView 等）。凡是可滚动/可滑动的，都要套 `ScrollConfiguration(behavior: 允许 mouse/trackpad 的 behavior)`。`meditation_page.dart` 和 `calm_drawer_page.dart` 里都有 `_MouseDragScrollBehavior` 可参考。
4. **半透明填充别加 BoxShadow**：CSS 的 box-shadow 会裁掉盒内部分，Flutter 不会 → 阴影透过半透明填充显示成灰黑方块（首页心情卡踩过这个坑）。
5. **无限循环动画的测试**：不能用 `pumpAndSettle`（永不停），改用固定时长 `pump(Duration(...))`。

---

## 4. 从 Figma 取素材的工作流（重要，已跑通）

App 里所有插画/图标都是从 Figma 直接取的**真透明 PNG**。方法：

1. 用 MCP 工具 `mcp__…__get_design_context`（fileKey + nodeId）拿某帧的精确布局 + 素材 URL + 文本。
2. 用 `download_assets`（fileKey + nodeId）拿节点的**原始源图**（rawImages）——这些**自带 alpha 通道，是真透明的**（比对着预览截图干净得多）。
3. `curl` 下载临时 URL → 用 PIL（Python）切图/去背景 → 存进 `assets/<模块>/`。
4. 精灵图切多个图标时：**先分行再分列**或按 alpha bbox 切；挑「竖版大图」当背景时按宽高比挑，别按面积（会误选横向精灵表）。
5. 新增 asset 目录后要在 `pubspec.yaml` 的 `flutter: assets:` 下登记，然后 `flutter pub get`。

> 坑：ChatGPT 生成的原图（`picture/` 里那些）很多是**烤了暗背景或棋盘格**的，程序抠不干净（白云和棋盘白同色）。**优先用 Figma `download_assets` 的 rawImages**，那才是真透明。

素材目录：`assets/{weather_icons,weather_glyphs,mascots,nav,home,healing,my,meditation,calm}/`

---

## 5. 代码结构

```
lib/
  main.dart                  App 入口 + _PhoneFrame(393×852) + RootShell(4 Tab)
  models/
    weather.dart             7 天气枚举(stormy..bright) + score映射 + 三套图标路径
    diary.dart               日记模型 + 日期键
  data/
    diary_repository.dart     日记存储接口 + 本地实现 + isWritable(7天补写窗口)
  widgets/
    app_bottom_nav.dart       底部 4 Tab 导航
    mood_mascot.dart          写日记滑块的吉祥物占位（可换他人组件）
    mood_ruler_slider.dart    写日记的尺子滑块（7档天气）
  pages/
    home_page.dart            Weather mood 首页
    write_diary_page.dart     写日记（滑块选天气+媒体占位+Post）
    report_page.dart          Report（两周日历+折线卡+成就+认知偏差）
    mood_chart_page.dart      心情变化图表弹窗（纵轴7心情/横轴日期）
    healing_page.dart         Healing 六卡网格
    meditation_page.dart      2-min Meditation（方法选择+呼吸/5-4-3-2-1+FeelBetter+Cozy）
    calm_drawer_page.dart     12h Calm Drawer（写→设时长→锁定→冷却→查看）
    my_page.dart              My（头像/VIP/徽章/菜单）
```

天气有**三套图标**（见 `weather.dart`）：`iconAsset`(彩色方块，写日记Switch mood)、`glyphAsset`(透明发光，主页/日历)、`mascotAsset`(吉祥物形象，滑块面板)。

---

## 6. 功能进度

| 模块 | 状态 | 说明 |
|---|---|---|
| Weather mood 首页 | ✅ | 日期/加号/心情卡/Quiz入口/两张历史卡 |
| 写日记 | ✅ | 滑块选7天气→映射score，媒体=Photo/Video选择框(占位)，地点=占位 |
| Report | ✅ | 两周日历(table_calendar)、Mood chart 弹窗、成就(占位柱)、认知偏差(占位气泡) |
| My | ✅ | 资料/VIP/5徽章/Privacy·Notifications·Setting(占位) |
| VIP 订阅(Sub) | ✅ | 从 My 的 VIP 标签进入；3 套餐单选；Subscribe 待接支付 |
| Healing 主页 | ✅ | 2×3 六卡 |
| ├ 2-min Meditation | ✅ | 方法选择卡 + Tidal Rhythm(4-7-8呼吸动画) + Reality Anchor(5步grounding,每步独立背景) + Feel Better + Cozy |
| ├ 12h Calm Drawer | ✅ | 5屏流程 + 本地存储 + 实时倒计时 + 手动选时长(滚轮) + Skip解锁保留内容 |
| ├ Cognitive Correction | ✅ | CBT 5 步（写困扰→选偏差→现实检验→重构→完成）+ 历史记录页（本地存储，右上时钟入口） |
| ├ AI Tree Hole | ⬜ | 未做，需接大模型 |
| ├ Reply Templates(轻沟通) | ⬜ | 未做，需接大模型 |
| ├ 1-on-1 Therapy | ⬜ | 未做，需接支付 |
| Quiz（首页蓝按钮） | ⬜ | Solo/Co-op 全未做 |
| 账户/Supabase | ⬜ | 全本地，未接云 |

---

## 7. 关键产品决策（已敲定，见 `v1_设计决策记录.md`）

- 市场=**海外**（英文 UI，地图用 Google，支付 Stripe，大模型用 Claude/GPT）。
- 日记：**一天一篇**，**7 天补写窗口**，天气 7 档 `stormy/rainy/gloomy/hazy/breezy/sunny/bright` 映射 score 1–7，只填天气不单独填分数。
- 折线图：X 轴真实日期，≤3 天实线、>3 天虚线。
- 账户：计划**匿名进入 + 延迟绑定 Google**，尚未实现。
- 媒体：v1 用 Supabase public bucket + 服务端显式删除（上量前迁 private）——未实现。

详见根目录 `v1_设计决策记录.md`（v1 范围、diaries 表结构等）和 `功能设计文档_完整版.docx`（原始完整需求，6 大模块）。

---

## 8. 已知待办 / 技术债

- 媒体上传、地点选择（Google Places）、AI 子功能、支付、Quiz、Supabase 全部待做。
- 部分卡片是**占位**（Report 的成就柱/认知偏差气泡、My 的菜单项、写日记的媒体/地点）。
- 音频（冥想背景音乐）未接。
- `picture/` 93MB 源图**未入库**（.gitignore 排除）；两个 `.docx` 和根目录截图也未入库。

---

## 9. 继续开发（★ 新对话直接粘贴下面这段）

```
我在继续开发一个 Flutter Web 项目 Mood Quiz（情绪/关系 App）。请先读这两个文件了解全部上下文：
- D:\projectd\mood_quiz\开发文档.md （开发交接文档，含架构/约束/进度）
- D:\projectd\mood_quiz\v1_设计决策记录.md （产品决策）

关键事实：
- Flutter 工程在 D:\projectd\mood_quiz\mood_quiz\（双层目录）。
- 本地跑：cd 到工程目录，flutter run -d web-server --web-port 8977 --web-hostname 127.0.0.1 --no-web-resources-cdn，然后开 http://127.0.0.1:8977。改代码要重启。
- 所有 UI 按 Figma 像素还原，当前设计文件 APP_Haze，fileKey=ldx4kyok408yp6dMgDBy0F，用 Figma MCP 的 get_design_context / download_assets 取规格和真透明素材。
- 页面统一 393×852 画框（main.dart 的 _PhoneFrame），新页面直接按该坐标布局。
- Web 上可滚动组件要套允许鼠标拖动的 ScrollBehavior（参考 calm_drawer_page.dart 的 _MouseDragScrollBehavior）。
- 每次改完跑 flutter analyze（须0问题）+ flutter test（当前22通过），再重启 server 让我验证；我确认后再 commit+push（push 会自动部署到 GitHub Pages）。
- 提交到公开仓库前，涉及大文件/文档的范围先跟我确认。

我这次要做的是：<在这里写你要做的功能，例如「Healing 的 Cognitive Correction 子功能」或「首页 Quiz 入口」或「接 Supabase」>
```

---

## 10. 常用命令速查

```bash
# 跑（本地 Web）
cd D:\projectd\mood_quiz\mood_quiz
flutter run -d web-server --web-port 8977 --web-hostname 127.0.0.1 --no-web-resources-cdn

# 检查 + 测试
flutter analyze
flutter test

# 提交 + 推送（自动部署）
cd D:\projectd\mood_quiz
git add . && git commit -m "..." && git push

# 看部署状态
# 仓库 Actions 页，或 https://usercjr.github.io/mood_quiz/
```
